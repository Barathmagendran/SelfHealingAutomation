# ==============================================================
# QA Environment Configuration
# Overrides base config.properties for QA runs
# ==============================================================

env=qa
base.url=https://flipkart.com
api.base.url=https://api.qa.example.com

# QA-specific credentials (use secrets manager in production)
api.auth.token=qa-bearer-token-here
api.username=qa_user
api.password=qa_password
